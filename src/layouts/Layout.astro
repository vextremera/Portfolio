---
import { ClientRouter } from "astro:transitions";
import "../styles/global.css";

// interface Props {
// 	title: string;
// }

// const { title } = Astro.props;
---

<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<!-- <title>{title}</title> -->
		<ClientRouter />
	</head>
	<body>
		<!-- Fondo absoluto de bolas erráticas animadas -->
		<div class="fixed inset-0 -z-10 pointer-events-none">
		<div id="balls-bg" class="w-full h-full"></div>
		</div>
		<!-- Blur efect -->
		<div class="fixed inset-0 -z-5 pointer-events-none backdrop-blur-xs"></div>

		<div>
			<slot />
		</div>
	</body>
</html>

<script>
  import "../scripts/background.js"
</script>

<style is:global>
	html,
	body {
		margin: 0;
		width: 100%;
		height: 100%;
	}

	body {
		@apply grid place-content-center;
	}

	body > div {
		@apply container;
	}
</style>
